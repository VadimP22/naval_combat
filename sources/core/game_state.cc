#include <iostream>
#include <vector>

#include "core/game_state.h"


void core::GameBoard::DebugPrint() {
    for (int row = 0; row < 100; row++) {
        for (int col = 0; col < 100; col++) {
            // board_.
        }
    }
}